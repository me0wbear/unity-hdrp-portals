using UnityEngine;

namespace UHFPS.Runtime
{
    // Отдельный MonoScript нужен для постоянной ссылки после сохранения сцены.
    // Это лабораторная заглушка, не реализация UHFPS.
    public class PlayerStateMachine : MonoBehaviour
    {
        public Vector3 Motion;
    }
}
